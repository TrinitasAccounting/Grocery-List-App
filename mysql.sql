

-- CREATE TABLE todos(todoid INTEGER NOT NULL PRIMARY KEY, name TEXT NOT NULL, isComplete NOT NULL);

-- INSERT INTO todos(todoid, name, isComplete)
-- VALUES (3,"Walk Dog", False);

-- SELECT * FROM todos;